# Ignore backend/server files - backend is deployed on Render
api/

# Ignore build outputs that aren't needed
dist/api/

# Ignore server config files
tsconfig.server.json
render.yaml

# Ignore development files
node_modules/
.env
.env.*

# Keep shared/ folder (needed by frontend)
# Keep dist/public/ (build output)
